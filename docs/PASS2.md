# 新增統合主頁：本機 Qwen 複核

模型 qwen3.8:27b-mlx，Ollama 本機端點。原始輸出 `qwen-audit.md`／`qwen-audit.json`。新增主頁尚未公開，因此沒有將其源码送外部模型；視覺原則沿用本輪 Codex、Gemini、Qwen 的七站稽核。

| 項目 | 複核結果 |
| --- | --- |
| H1：↗ 未加「新分頁」提示 | 剔除。課程連結實際沒有 target=_blank，在同一分頁前往外站；箭頭不代表新分頁。只有實際 target=_blank 的三個社群連結提供新分頁提示。已驗證 DOM。 |
| H2：「整合七個課程」可能暗示所有教材核准 | 不採改字。「整合」描述導覽網站，不是醫療審閱；每張卡片及列表下方已有審閱提示。保留課程入口與醫療審閱的明確區別，不加虛構核准。 |
| H3：部分卡片提示不同且髖部缺少待審閱標示 | 剔除事實錯誤。髖部實際有「含待審閱教材，詳見單元標示」。肩與膝採「請依單元審閱標示學習」，沒有宣稱全部內容核准。醫師對象與教材狀態不構成邏輯矛盾。 |

主 agent 實測並修正：快速分類的錨點預設動作會移走標題焦點，已改為明確更新 fragment、捲動及焦點；七張卡片改為上肢 2、下肢 3、脊椎 2 分組，避免最後一列的孤立卡片；手機導覽具有 44px 高觸控範圍；列印時恢復所有分類。

`validation.json`：8 組主題／視窗配置、分類及快速入口、七個正式 href、3 個追蹤 href、主題保存、SkipLink、無 JavaScript 全部入口，以及本機 preview 輸出隔離。
